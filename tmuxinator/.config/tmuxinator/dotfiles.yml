name: dotfiles
root: ~/workspaces/dotfiles/

windows:
  - neovim-dotfiles:
      panes:
        - nvim .
  - neovim-local:
      root: ~/workspaces/local_config/
      panes:
        - nvim .
  - shell:
